

 
 ////////////////
 git config --global user.name "John Doe"
 git config --global usergit.email johndoe@example.com
git config --global core.editor emacs
 git config --global merge.tool vimdiff
 
/// create git master



#find -name  .git  |  xargs  rm -r

rm -rf `find -name CVS`

git init
git add -f * 
git commit -a -m "init"
git config --bool core.bare true


rm -rf *

sudo rm -rf `find -name .gitig*`

git add -f * 
git commit -a -m " dev "
git push



git commit -m "dev" *.sh


cd ..
git clone .ssv_gito  ssv_git

cp -r .metadata git/

cp -r .git ~/git/.git_kafka_adapter


////////////
git commit -m "v0"
 git commit -a -m"test"
 
 git mv file_from file_to
 git remote -v
 git branch testing
 git co testing
 

git commit -m ’initial commit’
git add forgotten_file
git commit --amend
git remote -v
-bash-3.2$ git checkout bid  dnetcom load_attrib netacuity_tool protobufs ssv ssv_protobufs syscom yahoo bidder dbproxy exstream fx_net load_pqs prizm  RestfulApi ssv_base  ssvwr zeromq
